{
  "name": "julians-guide-to-interfaces",
  "version": "0.53.0",
  "private": true,
  "description": "An evolving, agent-friendly handbook of interface design principles.",
  "scripts": {
    "dev": "node server.mjs",
    "check": "node --check script.js && node --check server.mjs",
    "build:ds": "esbuild design-system/app.jsx --bundle --minify --format=esm --jsx=automatic --outfile=design-system/app.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright-core": "^1.62.1"
  },
  "dependencies": {
    "@base-ui/react": "^1.7.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
